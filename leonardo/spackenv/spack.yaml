# This is a Spack Environment file.
#
# It describes a set of packages to be installed, along with
# configuration settings.
spack:
  # add package specs to the `specs` list
  specs: [compss, py-pip]
  view: true
  concretizer:
    reuse: false
    unify: true
  repos:
  - /leonardo/home/userexternal/vmagni00/spack-compss/spack-dt-geo/var/spack/repos/builtin
